name: gdar_fruit
description: Apple Liquid Glass styles for GDAR.
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  shakedown_core:
    path: ../../shakedown_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  shaders:
    - shaders/steal.frag
