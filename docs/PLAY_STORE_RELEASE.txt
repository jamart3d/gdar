What's new in v1.3.62+272
- Jules merge (`b87e230`, `a1f70b4`) broke Linux checkup: `preflight_check.dart` treated Linux as Chromebook, `checkup.md` removed `LINUX:VERIFIED` from proceed condition, `audit_assets.sh` scanned a non-existent `assets/` dir. All three restored.
- Merged locally to `main` in `2791a6b`, then completed the parked follow-up
- Autocorrelation beat detection refactored (VisualizerPlugin.kt):
  - Removed coarse 20Hz fallback RMS path (±30 BPM resolution — too coarse to be useful)
  - Fixed unconditional BPM override: autocorr now only overrides `trackedBeatBpm` when `beatGridConfidence < 0.4`
  - Capped O(n²) correlation loop at 256 samples with documented cost bound
  - Implemented second-pass refinement with two modes: parabolic interpolation (Mode A, cheap) and 4× upsample narrow-window re-search (Mode B, HQ)
  - Sabrina (Chromecast with Google TV) hardware gate: Mode B always disabled on Sabrina regardless of Flutter setting
  - Extracted `useAutocorr` as a single local variable (was duplicated for `beatBpm` and `beatIbiMs` — divergence risk)
  - `corrValues` now allocated only for Mode A; Mode B and no-second-pass skip the allocation
  - Mode B inner loop `-1` bound documented (ensures `lo+1` stays in bounds)
  - Removed `!!` on `autocorrIbiMs`; replaced with local `ibi` variable
- New SettingsProvider prefs: `beat_autocorr_second_pass` (TV default: on), `beat_autocorr_second_pass_hq` (default: off everywhere)
- New TV Settings → Appearance toggles: "Beat Precision Refinement" and "High-Quality Refinement" (HQ shown only when refinement is enabled)
- `updateConfig` pipeline extended end-to-end: `AudioReactor` interface → `VisualizerAudioReactor` → `screensaver_screen` → `tv_screensaver_preview_panel`
- `FakeSettingsProvider` and `FakeAudioReactor` test stubs updated for new interface
- While restoring pre-existing dirty `main` work, the `VisualizerPlugin.kt`
  - the new second-pass autocorrelation / confidence-gating logic
  - the snapshot-based stereo RMS read via `StereoCapture.getRmsSnapshot()`
- TV stereo RMS hardening implemented from [`docs/superpowers/plans/2026-04-05-stereo-capture-rms-hardening.md`](docs/superpowers/plans/2026-04-05-stereo-capture-rms-hardening.md):
  - `StereoCapture.kt`: moved RMS history size to companion object as `RMS_HISTORY_SIZE`
  - `StereoCapture.kt`: privatized RMS history state, added dedicated RMS lock, and added `getRmsSnapshot()` returning oldest→newest snapshot copies
  - `StereoCapture.kt`: reset path now clears RMS ring-buffer state under the same lock used for reader/writer synchronization
  - `StereoCapture.kt`: removed fixed `SAMPLE_RATE`/`RMS_BLOCK_SIZE`; `start()` now accepts a preferred sample rate, then derives effective `capturedSampleRate` from `AudioRecord.sampleRate` and recomputes `rmsBlockSize`
  - `MainActivity.kt`: queries `AudioManager.PROPERTY_OUTPUT_SAMPLE_RATE` and passes it into `stereoCapture.start(...)`
  - `VisualizerPlugin.kt`: replaced direct reads of `fullRmsHistory`/`rmsHistoryIndex`/`rmsHistoryCount` with `getRmsSnapshot()`
- Added Android JVM regression test setup for TV native code:
  - `apps/gdar_tv/android/app/build.gradle.kts`: added `testImplementation("junit:junit:4.13.2")`
  - `StereoCaptureTest.kt`: covers empty-history snapshot and wrapped ring-buffer ordering
- Plan doc updated during implementation to tighten Step 7 and document the effective-sample-rate approach
- Fruit car mode stat-chip typography increased without changing chip height or row layout:
  - `playback_screen_fruit_widgets.dart`: reduced the internal label→value gap from `10 * scaleFactor` to `8 * scaleFactor`
  - `playback_screen_fruit_widgets.dart`: increased stat value text from `17 * scaleFactor` to `18 * scaleFactor`
  - This was the largest clean bump that fit the existing `74`-pixel chip without overflow; larger candidates (`20` and `24`) overflowed during TDD
- Added widget regression coverage:
  - `playback_screen_test.dart`: new test `PlaybackScreen Fruit car mode increases stat value typography without overflow`
  - Test asserts representative long value `1200ms` still renders and that the stat value font size is `18.0`
- Merged locally to `main` in commit `65e26b5`
- Web Fruit car mode stat chips were reworked to improve legibility when Liquid Glass is enabled:
  - `playback_screen_fruit_widgets.dart`: value text is now `24 * scaleFactor`
  - `playback_screen_fruit_widgets.dart`: internal vertical padding reduced to `6 * scaleFactor`
  - `playback_screen_fruit_widgets.dart`: label→value gap reduced to `2 * scaleFactor`
  - `playback_screen_fruit_widgets.dart`: added a value-only lens treatment (`fruit_car_mode_stat_value_lens`) behind the number when `fruitEnableLiquidGlass` is on
  - `playback_screen_fruit_widgets.dart`: lens path applies a slight `Transform.scale(1.03)` boost to the value text for a magnified watch-crystal read
- Test support and regression coverage updated:
  - `playback_screen_test.dart`: `MockSettingsProvider` now supports `setFruitEnableLiquidGlass(bool value)`
  - `playback_screen_test.dart`: replaced the earlier size-only stat-chip regression with `PlaybackScreen Fruit car mode shows a magnified value lens when glass is enabled`
  - The new test asserts long value `1200ms` still renders, the value font size is `24.0`, four lens overlays are present, and no layout exceptions occur
- Handover summary saved to:
  - `docs/superpowers/specs/2026-04-05-fruit-car-mode-stat-lens-handover.md`
- Evaluated `reports/2026-04-05_14-46_v1.3.61+271_fruit_car_mode_stalled_playback.md` against the current codebase on `94c1f8e`
- Confirmed the Fruit car mode screen is only a consumer of `positionStream` / `durationStream`; the likely fault domain is the web engine to Dart bridge, not Fruit UI layout
- Confirmed current web stack already has worker-tick and watchdog behavior in JS (`html5`, `hybrid`, and `webAudio` paths), so the report's original fix direction was too broad
- Identified `apps/gdar_web/web/hybrid_init.js` as the real engine selection path and noted that PWA/mobile often routes to HTML5 or hybrid, not pure Web Audio
- Wrote implementation plan:
  - `docs/superpowers/plans/2026-04-05-fruit-car-mode-stalled-playback.md`
- Plan scope:
  - add a root-cause regression test at the JS engine boundary
  - add a narrow Dart-side `getState()` resync path on visibility restore / stale tick
  - add Dart regression coverage for resumed progress updates
- No runtime fix has been implemented yet in this session
- Implemented and verified Task 1:
  - added `packages/shakedown_core/lib/services/buffer_agent_stall_policy.dart`
  - updated `BufferAgent` to use `10s` for visible web playback and `20s` otherwise
  - treated web `AppLifecycleState.inactive` as visible for both threshold selection and recovery visibility behavior
  - added `packages/shakedown_core/test/services/buffer_agent_stall_policy_test.dart`
- Implemented and verified Task 2:
  - added `GaplessPlayer.playBlockedStream` on web and native
  - bridged JS `onPlayBlocked` into Dart in the web player
  - added a dispose-safe `_emitPlayBlocked()` guard to avoid late callback writes after stream closure
  - added `packages/shakedown_core/test/services/gapless_player_play_blocked_stream_test.dart`
  - regenerated only the mock files that needed the new `playBlockedStream` getter
- Implemented and verified Task 3:
  - updated `audio_provider.dart`, `audio_provider_diagnostics.dart`, `audio_provider_lifecycle.dart`, and `audio_provider_state.dart`
  - `AudioProvider` resume prompts now use a transient playback-recovery path instead of seeding sticky issue state
  - `playBlockedStream` and `suspended_by_os` prompts now clear when playback reports `playing=true`
  - added provider coverage for the real recovery path:
    - `playBlockedStream sets the browser resume agent message`
    - `playBlockedStream prompt clears after playback resumes`
  - updated `packages/shakedown_core/test/ui/widgets/playback/playback_messages_test.dart`
  - fixed `packages/shakedown_core/test/verify_data_integrity_test.dart` so package-level test runs succeed from the monorepo root
- Session handoff saved to:
  - `.agent/notes/session_handoff.md`
- No commit was made in this session
- PWA startup three-layer flicker eliminated (report: `reports/2026-04-05_15-56_v1.3.61+271_pwa_startup_flicker.md`):
  - `manifest.json`: `background_color` `#000000` → `#080808` — aligns OS splash background with HTML splash
  - `index.html` `body`: `background-color` `#000000` → `#080808`
  - `index.html`: replaced `<img src="icons/Icon-512.png">` with `<div id="splash-title">Shakedown</div>` in RockSalt font (24px, weight 400, `#00E676`, `letter-spacing: 1.2px`) — matches Flutter `ShakedownTitle` to eliminate layout snap at HTML→Flutter seam
  - `index.html`: RockSalt font preloaded via `<link rel="preload">` + `@font-face` (`font-display: block`, correct descriptor weight 400)
  - `index.html`: added indeterminate `#00E676` sweep progress bar (`#splash-progress-track` / `#splash-progress-fill`) using `transform: translateX` animation — GPU-composited, loops continuously, fades with splash (no snap risk)
  - `index.html`: splash fade reduced from 700ms total (100ms delay + 600ms) to 200ms; pre-hide delay removed
  - `index.html`: returning-user fast path via `localStorage` key `gdar_pwa_visited` — 100ms fade on cached app revisit vs 200ms on first visit
  - `hybrid_init.js`: `gdar_pwa_visited` added to `?flush=true` allowlist
- Commits: `11721e0` → `dd3fb99` (7 commits, web-only, no Dart changes)
- Autocorr merge tip verified in an isolated worktree before integration:
  - `dart run melos exec -c 1 --dir-exists=test --ignore=screensaver_tv -- flutter test`
- Autocorr follow-up settings/runtime commit verified before integration:
  - WIP was committed in `.worktrees/feat-autocorr-improvements` as
  - `dart run melos exec -c 1 --dir-exists=test --ignore=screensaver_tv -- flutter test`
- `git diff --check` passed for the updated Kotlin, plan, and Android test files
- `flutter build apk --debug` reached Android SDK configuration, then failed on host setup before Kotlin compilation:
  - NDK package `ndk;28.2.13676358` license not accepted
  - this Chromebook environment exposes `/usr/lib/android-sdk/platform-tools` only; `sdkmanager` is not installed on `PATH`
- Fruit car mode stat-chip change verified on merged `main`:
  - `flutter test packages/shakedown_core/test/screens/playback_screen_test.dart` passed (`11` tests)
  - `flutter analyze` passed with `No issues found!`
- Fruit car mode stat-lens follow-up verified in `feat/fruit-car-mode-stat-font-size-v2`:
  - `flutter test packages/shakedown_core/test/screens/playback_screen_test.dart` passed (`11` tests)
  - `flutter analyze` passed with `No issues found!`
- Web buffer agent recovery work verified in `web-buffer-agent-recovery` worktree:
  - `git diff --check` passed
  - `flutter test packages/shakedown_core/test/services/buffer_agent_stall_policy_test.dart` passed
  - `flutter test packages/shakedown_core/test/services/buffer_agent_test.dart` passed
  - `flutter test packages/shakedown_core/test/services/gapless_player_play_blocked_stream_test.dart -r compact` passed
  - Dart MCP analyze of `packages/shakedown_core/lib` and `packages/shakedown_core/test` returned no errors
  - Dart MCP full `packages/shakedown_core/test` run passed

---

What's new in v1.3.61+271
- Moved the Fruit web `Keep Screen On` toggle out of Playback and into Interface, directly under `Floating Spheres`.
- Made Fruit tab transitions animate in car mode even when liquid glass is off, using a shorter fallback transition instead of an instant page jump.
- Made the Fruit car-mode top stat chips freeze on their last live values while playback is paused, then resume updating on play.
- Reworked the Fruit car-mode upcoming queue area into a scrollable region so longer queues stay reachable without pushing controls off-screen.
- Added regression coverage for paused stat-chip freezing and car-mode tab animation behavior.
- Fixed an active Fruit car-mode show-list card overflow on narrower widths by tightening the internal layout instead of clipping content.
- Made narrow Fruit car-mode show-list cards compact the weekday first, then omit the weekday entirely when the card gets too tight.
- Doubled the compact embedded mini-player play/pause button size on the active show card and increased the thickness of the current-show duration/progress indicator.
- Disabled `--wasm` in the active local web helper/workflow docs and switched the documented Fruit web run/build paths back to standard Chrome web commands.
- Split the oversized Fruit playback builder into focused part files:
  - `playback_screen_fruit_build.dart`
  - `playback_screen_fruit_car_mode.dart`
  - `playback_screen_fruit_widgets.dart`
- Split the oversized Fruit show-list card builder into focused part files:
  - `show_list_card_build.dart`
  - `show_list_card_fruit_car_mode.dart`
  - `show_list_card_fruit_mobile.dart`
- Fixed the related analyzer findings uncovered during the refactor cleanup.
- Refreshed `.agent/notes/verification_status.json` to `PASS` for the current `HEAD`.
- Added a fresh 2026-04-03 monorepo scorecard documenting the improved validation signal, current hotspot list, and fresh Chrome runtime verification.
- Extracted the private `_FruitFloatingSpheres` / `_FruitFloatingSpheresPainter` / `_FruitSphereNode` classes into a new public shared widget: `packages/shakedown_core/lib/ui/widgets/backgrounds/floating_spheres_background.dart`.
- Introduced the `SphereAmount` enum (`tiny(6)` / `small(12)` / `medium(22)` / `more(38)`) to control sphere density declaratively.
- Refactored Fruit car mode to import `FloatingSpheresBackground` with `sphereCount: SphereAmount.tiny` — exact visual parity preserved.
- Added `enableTvBackgroundSpheres` (bool, default `false`) and `tvBackgroundSphereAmount` (String, default `'small'`) to `SettingsProvider`, `DefaultSettings`, and the shared-preferences loading path.
- Mounted the sphere background as the bottommost `Positioned.fill` child inside the `TvDualPaneLayout` Stack, wrapped in `IgnorePointer` + `RepaintBoundary`, gated by `enableTvBackgroundSpheres`.
- Added TV-only settings UI in the Interface section: a toggle tile ("Background Spheres") and a `SegmentedButton` (Small / Medium / More) that appears when the toggle is on.
- Exported `FloatingSpheresBackground` and `SphereAmount` from the `shakedown_core` barrel.
- Sphere generation for counts > 6 uses a seeded-deterministic RNG with three depth bands (far / mid / near) for a layered ambient feel; Fruit's original 6-sphere look is preserved via fixed `const` seeds.
- Full monorepo analysis passes with zero errors after implementation.

---

What's new in v1.3.60+270
- Added a persisted `car_mode` setting in `SettingsProvider`, defaulting to off.
- Added a Fruit web `Car Mode` toggle below `UI Scale` in Interface settings.
- Made `Car Mode` force `UI Scale` off, and hide the `UI Scale` toggle while `Car Mode` is enabled.
- Moved the Fruit web `Keep Screen On` toggle out of Playback and into Interface, placing it directly under `Floating Spheres`.
- Added targeted tests covering `carMode` persistence, `UI Scale` shutoff, settings visibility, and larger Fruit tab bar sizing in car mode.
- Added a dedicated Fruit playback car-mode layout that activates only when `carMode` is enabled.
- Reworked the car-mode playback hierarchy to use a large metadata-first layout with venue, location, date, current track, progress, controls, and upcoming tracks in that order.
- Added a toggleable top chip row in car mode with two states:
  - playback stats: `DFT`, `HD`, `NXT`, `LG`
  - metadata: rating, `SRC`, and `SHNID`
- Made the top chip row always visible in car mode, even when Audio HUD is off.
- Removed labels from the metadata row, removed the card boundary around rating stars, and stabilized the right-side chip sizing across toggle states.
- Added a more visible buffered-progress rail inside the car-mode progress bar, changed the right readout from remaining time to total duration, and restored the pending prebuffer pulse cue.
- Enlarged car-mode metadata typography, moved the controls closer under the progress bar, and expanded the upcoming queue to show up to four tracks with stepped emphasis.
- Added optional passive floating spheres for Fruit playback car mode, with a settings toggle and performance safeguards:
  - reduced blur
  - stepped animation updates
  - static behavior in `performanceMode`
  - continuous drift motion without cycle resets
- Improved the car-mode play/pause button treatment when Fruit liquid glass is enabled.
- Made Fruit bottom-tab transitions animate in car mode even when liquid glass is off, using a shorter fallback transition instead of an instant page jump.
- Replaced the car-mode metadata chip pair with a plain stacked text block so `SRC` sits above `SHNID` with no chip borders or backgrounds.
- Doubled the visible size of the car-mode rating stars and expanded the tappable rating zone so tapping anywhere around the star cluster opens the rating dialog.
- Made the Fruit car-mode top stat chips freeze on their last live values while playback is paused, then resume updating on play.
- Reworked the Fruit car-mode upcoming queue area into a scrollable region so longer queues stay reachable without pushing controls off-screen.
- Added focused playback and tab-bar test coverage for the car-mode layout and behavior.
- Added a Fruit library/show-list car-mode treatment for web that activates when `carMode` is enabled.
- Gave the Fruit library header, spacing, and list density a larger car-mode presentation.
- Added a dedicated taller Fruit car-mode show-list card layout with larger venue, location, date, rating, and source/badge affordances so fewer shows are visible at once.
- Made Fruit library car-mode cards respect the existing `dateFirstInShowCard` setting.
- Reworked the car-mode show-list card footer so source metadata stacks under the rating area and the embedded mini player gets a wider right-aligned slot for larger duration/progress presentation.
- Fixed an active Fruit car-mode show-list card overflow on narrower widths by tightening the internal layout instead of clipping content.
- Made narrow Fruit car-mode show-list cards compact the weekday first, then omit the weekday entirely when the card gets too tight.
- Doubled the compact embedded mini-player play/pause button size on the active show card and increased the thickness of the current-show duration/progress indicator.
- Added focused show-list test coverage for the Fruit car-mode card branch and kept the Fruit show-list contract tests passing.
- Disabled `--wasm` in the active local web helper/workflow docs for now and switched the documented Fruit web run/build paths back to standard Chrome web commands.

---

What's new in v1.3.59+269
- TV/Screensaver: Fixed a native PCM freshness bug in `VisualizerPlugin.kt`
- TV/Android: Verified the native fix compiles cleanly with
- Android/Release: Mirrored PCM capture hardening into the shipping
- Android/Release: Added stricter fail-closed guards around MediaProjection
- Docs: Updated `.agent/specs/tv_screensaver_spec.md` to note that native TV
- Investigation: Confirmed the remaining Sabrina risk, if Play-installed

---

What's new in v1.3.58+268
- **Maintenance**: General maintenance and version synchronization.

---

What's new in v1.3.57+267
- **Maintenance**: General maintenance and version synchronization.

---

What's new in v1.3.56+266
- Fixed TV main screen scrollbar visibility bug in
- Added widget tests for the above fix in
- Fixed test deadlock in
- Fixed screensaver-dismiss-on-PCM-confirm bug in
- Refactored `packages/shakedown_core/lib/steal_screensaver/steal_graph.dart`
- Updated `docs/monorepo_todo_2026-04-02.md` to mark the `steal_graph.dart`
- Verified the refactor with

---

What's new in v1.3.55+265
- Web UI / Fruit playback: fixed the floating now-playing inset so the track
- Web UI / Fruit curation: rating stars now use fixed curation yellow
- Web UI / Fruit RGB: active playback RGB borders now continue to work when
- Web UI / Fruit playback player: the Fruit now-playing / player card now uses
- Specs and regression tests were updated for the Fruit playback inset, rating
- Monorepo docs: added a scorecard-derived todo list, refreshed the 2026-04-02
- Validation: captured a fresh serial analyzer pass and standard web release
- TV screensaver settings maintainability: split the large TV screensaver
- Screensaver banner maintainability: split `StealBanner` into dedicated flat
- TV screensaver PCM stereo capture (Android 14): fixed `MediaProjection`
- TV screensaver PCM beat detection: reset `_hasAttemptedStereoCapture` on
- TV screensaver PCM beat detection: increased consent dialog timeout from 10s
- TV screensaver PCM beat detection: widened the PCM freshness window from
- Tests: added `screensaver_screen_test` case verifying that a failed stereo

---

What's new in v1.3.54+264
- Added `oilPreviewShowGraph` preference (`oil_preview_show_graph` key, default `false`) to `SettingsProvider` / `default_settings.dart` / `settings_provider_initialization.dart`.
- New `_ToggleRow` in `TvScreensaverSection` (`_buildAudioGraphSection`) labelled **"Preview: Audio Graph"** — hidden when audio graph mode is `off`.
- `TvScreensaverPreviewPanel` now reads `oilPreviewShowGraph`: toggle ON → audio graph scaled to preview box, logo hidden; toggle OFF → logo shown, audio graph hidden.
- `setOilAudioGraphMode('off')` auto-resets `oilPreviewShowGraph` to `false`.
- `FakeSettingsProvider` updated with stub implementations.
- 4 new widget tests covering toggle visibility per audio graph mode value.
- **Fixed crash when confirming MediaProjection consent dialog on Chromecast with Google TV (Sabrina, Android 14).** Root cause: `getMediaProjection()` was deferred to an async `runWhenReady` callback, but Android 14 invalidates the consent intent once `onActivityResult` returns. Moved `getMediaProjection()` to run immediately inside `onActivityResult`, then the projection token is handed to `StereoCapture` after the foreground service is ready. (`MainActivity.kt`)
- **Fixed EventSink thread safety** — `VisualizerPlugin.processFftData()` now dispatches `eventSink.success()` to the main thread via `Handler(Looper.getMainLooper())`, preventing crashes from Visualizer callbacks firing on the audio thread. (`VisualizerPlugin.kt`)
- Inserted a narrow (28 px) vertical stereo LED VU strip between the two analog VU needle meters.
- Analog meter gap widened from 10 px → 44 px → 60 px (final) to clear both meter panels.
- Strip: 2 columns (L / R), 16 segments, teal / yellow / red zones from bottom to top, matching standard VU dB scale.
- Peak-hold indicators per channel, matching the analog meter peak-dot decay behaviour.
- Constants added: `_ledStripWidth`, `_ledSegmentCount`, `_ledLabelReserve`, `_ledColGap`, `_ledSegGap`, `_ledHPad`.
- `_drawLedStrip(Canvas, double cx, double baseY)` + `_drawLedSegment` helpers added to `StealGraph`.
- **Bug fix:** double-drift offset in `_drawLedStrip` corrected — `cx` already carries `drift.dx`; removed redundant `+ drift.dx` from `stripLeft` and dropped the `drift` parameter from the method signature.
- Restored deleted comment above `lRange`/`rRange` declarations in `_renderVu`.

---

What's new in v1.3.53+263
- Web Fruit playback: constrained the loading pulse bead in the Fruit now playing
- Tests: added a Fruit playback widget regression that samples the animated

---

What's new in v1.3.52+262
- **TV: PCM/Enhanced audio capture crash on Chromecast with Google TV (Sabrina, Android 14)** — Android 14+ requires a `mediaProjection` foreground service to be running *before* `createScreenCaptureIntent()` is called. The old code started the service after the consent dialog, causing a `SecurityException` crash when the user confirmed the permission. Reordered the flow: foreground service starts first, waits for ready, then launches the consent dialog. (`MainActivity.kt`)
- **TV: Stale foreground-service ready state** — `MediaProjectionForegroundService.start()` now resets `isForegroundReady` so `runWhenReady` callers properly wait for the new service instance instead of firing immediately on stale state. (`MediaProjectionForegroundService.kt`)
- **TV: EventSink called from wrong thread** — `VisualizerPlugin.processFftData()` dispatches `eventSink.success()` to the main thread via `Handler(Looper.getMainLooper())`, fixing potential crashes from Android Visualizer callbacks firing on an audio thread. (`VisualizerPlugin.kt`)

---

What's new in v1.3.51+261
- TV main browse/playback surfaces now default to pure black for OLED, matching the splash screen and TV specs.
- Tightened TV RGB focus borders across the main screen: show list cards, track list rows, and the empty-state "Play random show" action now use cleaner, closer-fitting outlines.
- Removed the oversized empty-state play icon on the TV playback pane and rebuilt the random-show CTA as a cleaner bordered button.
- Swept TV settings focus styling for border cleanup, including the screensaver section, so toggles, segmented controls, steppers, and list rows use the same tighter focus treatment.
- Added a framed border container around the TV screensaver preview panel so it reads as a dedicated preview surface in Settings.

---

What's new in v1.3.50+260
- **Monorepo architecture migration: introduced `packages/gdar_design`**
- **Monorepo architecture migration: moved shared fonts into the design layer**
- **Monorepo architecture migration: detached style packages from feature code**
- **Monorepo architecture migration: repointed core to shared design primitives**
- **Monorepo architecture migration: restored app-layer theme assembly**
- **Monorepo architecture migration: removed temporary core-side theme bridges**
- **Updated the monorepo architecture plan to match the completed migration**
- **Validated the migration with focused app and regression tests**

---

What's new in v1.3.49+259
- **TV — Play Random Show button: D-pad focus flow** (`tv_dual_pane_layout.dart`)
  - `_focusRightPane()` now detects empty state (no show selected) and explicitly
  - Navigating **right** from the show list now reliably lands on the button.
- **TV — Play Random Show button: D-pad selectability** (`tv_playback_screen_build.dart`)
  - Replaced unfocusable `GestureDetector` with `TvFocusWrapper` + dedicated
  - Removed `autofocus: true` to prevent the button from stealing focus on mount.
- **TV — Play Random Show button: visual parity with header dice**
  - Both header dice and "Play random show" button now call the shared
  - `animationOnly: true` ensures `_currentShow` is **not** set immediately,
- **TV — Play Random Show button: styling**
  - Label forced to lowercase `'Play random show'` with an explicit `TextStyle`
  - Font size reduced to `10sp`; padding tightened (`16×8`) and border radius
  - Font resolved via `FontConfig.resolve(settingsProvider.activeAppFont)` to
- **TV — PlaybackScreenState: `focusRandomButton()` API** (`tv_playback_screen.dart`)
  - New public method and `_randomPlayFocusNode` added so `TvDualPaneLayout`

---

What's new in v1.3.48+258
- **TV Screensaver → Beat Detector: "Enhanced" (PCM) crash on confirmation** — Clicking the
- The stereo PCM beat-detection pipeline (`StereoCapture` → `VisualizerPlugin` PCM onset
- Once capture is active, a persistent notification badge ("Enhanced audio capture is active")
- Falls back to `HYBRID` detector automatically while PCM warms up (requires ≥ 12 analysis

---

What's new in v1.3.47+257
- Screensaver 4K limiting is now device-aware: only the 2020 Chromecast
- Removed `oilScreensaver4kSupport` user-facing setting from
- `DeviceService.isLowEndTvDevice` getter that checks
- New `screensaver_architecture.md` documentation replacing the
- `oilScreensaver4kSupport` field, getter, preference key, and
- `oil_screensaver_4k_support` SharedPreferences key (orphaned keys

---

What's new in v1.3.46+256
- Temporarily restored the older direct Android TV PCM approval path in [MainActivity.kt](/C:/Users/jeff/StudioProjects/gdar/apps/gdar_tv/android/app/src/main/kotlin/com/jamart3d/shakedown/MainActivity.kt):
- after the PCM permission dialog returns `OK`, the app now goes straight to `getMediaProjection()` and `stereoCapture.start(...)`
- this bypasses the `MediaProjectionForegroundService` startup layer to A/B test whether that service path is the source of the instant crash on Chromecast with Google TV 2020
- verified with `.\gradlew.bat :app:compileDebugKotlin`
- **[TV Screensaver Refinement]**
- Relocated live screensaver preview to the **TV Settings Navigation Rail**; now only renders when the Screensaver section is selected, optimizing CPU/Audio overhead.
- Implemented **4-Tap RGSS Anti-Aliasing** (Rotated Grid Supersampling) in `steal.frag` to eliminate logo "pixel crawl" when scaled.
- Refactored logo motion path to use **incremental path time**, ensuring smooth, continuous movement during flow speed adjustments with zero jumping.
- Implemented **Historical Trail Snapshots**; the trail now captures position, size (including bass-shaking pulses), and color for each ghost slice, creating a physics-based "ripple" wave.
- Fixed **Auto Palette Cycle** synchronization; background shader now correctly receives color updates from the game engine.
- Added verification tests for **Trail Snapshot** interpolation and **Preview Panel** lifecycle in `shakedown_core`.
- **[TV Random Show Flow Unification]**
- Unified the "Play Random Show" behavior across the TV UI to ensure all entry points (dice icon and playback pane buttons) initiate the premium animated sequence.
- Implemented **Delayed Playback Orchestration** for the right-pane empty state button; triggering it now correctly starts the header dice animation, auto-scrolls to the selection, and shifts focus before starting audio.
- Styled the TV playback empty state button with **Rock Salt Typography** and all-caps "PLAY RANDOM SHOW" to match the premium "Dice Control" aesthetic.
- Updated startup logic to respect the animated TV sequence if "Play Random Show on Startup" is enabled.
- Added unit and widget tests to verify the `delayPlayback` orchestration and focus-shifting logic.

---

What's new in v1.3.45+255
- Hardened the Android TV media-projection foreground-service path for `Enhanced` / PCM capture:
- in [MediaProjectionForegroundService.kt](/C:/Users/jeff/StudioProjects/gdar/apps/gdar_tv/android/app/src/main/kotlin/com/jamart3d/shakedown/MediaProjectionForegroundService.kt), wrapped `startForeground(...)` startup so failures log, notify waiting callbacks, and stop the service instead of crashing the app process
- in [MainActivity.kt](/C:/Users/jeff/StudioProjects/gdar/apps/gdar_tv/android/app/src/main/kotlin/com/jamart3d/shakedown/MainActivity.kt), treat “foreground service never became ready” as a graceful `false` result and tear down PCM capture state cleanly
- verified with `.\gradlew.bat :app:compileDebugKotlin`

---

What's new in v1.3.44+254
- Hardened Google TV `Enhanced` / PCM permission handling on Android TV:
- moved the explicit PCM permission request into TV settings when `Enhanced` is selected or when audio reactivity is enabled with `Enhanced` already selected
- kept non-interactive screensaver launches prompt-free
- Refined the Android native MediaProjection path in [MainActivity.kt](/C:/Users/jeff/StudioProjects/gdar/apps/gdar_tv/android/app/src/main/kotlin/com/jamart3d/shakedown/MainActivity.kt):
- fail closed if permission launch or foreground-service startup fails
- clear stale stereo-capture / foreground-service state before fresh PCM permission attempts
- reuse the same cleanup on stop, denial, and activity destroy
- Removed the temporary blanket Google TV / Chromecast block after hardware feedback showed the PCM permission dialog can succeed at least once, shifting the fix toward lifecycle hardening instead of device exclusion
- Hardened post-approval PCM capture in [StereoCapture.kt](/C:/Users/jeff/StudioProjects/gdar/apps/gdar_tv/android/app/src/main/kotlin/com/jamart3d/shakedown/StereoCapture.kt):
- wrapped the `AudioRecord` read loop so runtime failures log and exit instead of crashing the app
- treat negative read results as graceful failure
- avoid blocking `join(500)` on the UI thread during capture teardown
- Updated related tests and fakes:
- `AudioReactor.start()` test doubles now match the `Future<bool>` signature
- TV settings/screensaver tests cover the settings-owned `Enhanced` flow
- Verification completed in this session:
- `flutter test packages/shakedown_core/test/ui/widgets/settings/tv_screensaver_section_test.dart`
- `flutter test packages/shakedown_core/test/screens/screensaver_screen_test.dart`
- `flutter test packages/shakedown_core/test/steal_screensaver/steal_graph_test.dart`
- `.\gradlew.bat :app:compileDebugKotlin`

---

What's new in v1.3.43+253
- **Maintenance**: General maintenance and version synchronization.

---

What's new in v1.3.42+252
- fix(tv): hardened PCM permission flow and refined header layout
- fix(web): improved Audio HUD gap stability and restricted Android-theme players
- fix(ui): smoothed playback auto-scrolling with proportional safe-area scaling

---

What's new in v1.3.41+251
- fix(web): restored RANDOM dice animation in Fruit theme
- fix(web): improved transition gap reporting in Fruit Audio HUD

---

What's new in v1.3.40+250
- refactor(tv): extracted screensaver audio builder slice
- refactor(ui): split playback screen into focused part files
- chore(deps): minor dependency updates (logger, shared_prefs, build_runner)

---

What's new in v1.3.39+249
- Maintenance: General maintenance, workflow synchronization, and zero-friction orchestration updates.

---

What's new in v1.3.38+248
- Testing: Resolved NoSuchMethodError in Provider tests by injecting missing SettingsProvider stubs for hideTabText and fruitEnableLiquidGlass.
- UI/UX (Design Scanner): Replaced hardcoded Colors legacy values with semantic and hex equivalents to satisfy the micro-scanner guidelines across UI components.

---

What's new in v1.3.37+247
- Automation: Introduced process_hygiene.md rules and integrated them into the shipit workflow for better process management.
- Automation: Optimized shipit, checkup, and deploy workflows for higher zero-friction orchestration and status reporting.

---

What's new in v1.3.36+246
- TV UI: Implemented 'showScreensaverCountdown' setting to hide the debug inactivity timer by default on Google TV hardware.
- Maintenance: Updated mock implementations for robust testing and maintained 100% test pass rate across the monorepo.
- Versioning: Synchronized build number 246 across all app targets.

---

What's new in v1.3.35+245
- Maintenance: Completed a fresh monorepo health check and synchronized workspace state.
- Workflow: Executed the automated shipit release pipeline for v1.3.35, including sequential Android and Web production builds.
- Hygiene: Cleaned diagnostics and synchronized build numbers across all targets.

---

What's new in v1.3.34+244
- Maintenance: Successfully completed a comprehensive monorepo-wide health check with 258/258 tests passing across Mobile, TV, Web, and Core.
- Visual: Audited workspace assets and archived legacy high-resolution icon binaries to the archive/ directory to maintain a lean repository.
- Automation: Hardened the Zero-Friction Mandate for /checkup and /shipit workflows, ensuring rapid, sequential production builds and Firebase deployments.

---

What's new in v1.3.33+243
- Web Audio: Resolved "Abortion Collision" bug in the gapless engine where rapid track selection could inadvertently cancel its own fetch/decode request.
- Web UI: Added color-coded health warnings to visualizer buffering diagnostics to improve performance monitoring on low-power devices.

---

What's new in v1.3.32+242
- Core: Initialized production release workflow 'shipit' for automated monorepo builds and PWA deployment.
- Maintenance: Completed full workspace health check; synchronized versions across gdar_mobile, gdar_tv, and gdar_web.
- Optimization: Modularized and optimized `/checkup` and `/shipit` workflows with root-level workspace analysis, reducing pre-release health check time by over 95%.
- Core: Implemented platform-aware concurrency in Melos test scripts, enabling faster parallel verification on high-performance development machines.
- TV UI: Gated the screensaver countdown debug overlay with `kDebugMode` to ensure a clean visual experience in production builds.
- Maintenance: Resolved root `pubspec.yaml` workspace glob errors by explicitly listing styles packages, achieving zero-warning analysis across the monorepo.

---

What's new in v1.3.31+241
- Maintenance: Successfully completed full monorepo-wide health check with 258/258 tests passing across all targets.
- Hygiene: Cleaned workspace root of temporary diagnostic logs and artifacts.
- Testing: Resolved minor linting regressions in mobile test suite.

---

What's new in v1.3.30+240
- Web UI: Refined Fruit Now Playing Card and Track List with enhanced glassmorphism effects and layout polishing for better mobile responsiveness.
- Audio: Improved AudioProvider playback and state synchronization, including hardened gapless handoff handling for Web platforms.
- Testing: Updated AudioProvider test suite to cover new platform-specific playback logic and ensure 100% regression coverage.

---

What's new in v1.3.29+239
- TV UI: Resolved the issue where the screensaver countdown overlay was not visible on TV hardware.
- Inactivity Detection: Hardened the InactivityDetector and introduced a ScreensaverLaunchDelegate to ensure reliable screensaver triggering and visibility during extended sessions.

---

What's new in v1.3.28+238
- Core: Synchronized monorepo auto-approval policies and orchestration rules for zero-friction production releases.
- Maintenance: Updated internal rules to enforce stricter zero-friction autonomy and proxy confirmation.

---

What's new in v1.3.27+237
- Google TV: Added an InactivityDetector and debug countdown overlay in apps/gdar_tv to assist with hardware testing and ensure reliable screensaver triggers during long sessions.

---

What's new in v1.3.25+235
- Web UI: Refined HUD diagnostics for accurate sub-engine (WA/H5) and gap reporting.
- Web Audio: Fixed a track handoff race condition to ensure smooth transitions.
- Core: Added unnecessary_string_escapes lint to improve codebase hygiene.
- Maintenance: Fully stabilized the automated production preflight suite.

---

What's new in v1.3.24+234
- Web UI: Fixed a playback race condition where rapid play/pause could hang the audio engine.
- Web UI: Improved stability of volume fades and crossfades on the Fruit theme.
- Core: Refactored engine platform-gating to enable robust multi-platform testing in the Dart VM.
- Maintenance: Fully synchronized the monorepo preflight suite with 100% test pass.

---

What's new in v1.3.23+233
- Core: Modularized complex UI components (DevAudioHud, ShowListCard, TvScreensaverSection) for performance.
- Maintenance: Finalized internal monorepo deployment pipeline for automated releases.

---

What's new in v1.3.22+232
- Core: Advanced SettingsProvider refactoring into specialized service mixins for performance.
- Maintenance: Achieved zero-friction monorepo orchestration for the /deploy workflow.
- TV UI: Stabilized unified inactivity detection and removed redundant diagnostic tests.
- Web UI: Refined hardware-aware profile detection for PWA and narrow-mobile viewports.

---

What's new in v1.3.21+231
- Maintenance: Synchronized monorepo auto-approval policies and orchestration rules for zero-friction production releases.
- Audit: Conducted a unified monorepo health audit and staged results in reports/audits/.
- Web UI: Refined performance hint initialization for PWA and narrow-mobile viewports.

---

What's new in v1.3.20+230
- TV UI: Simplified inactivity detection for Google TV by removing brittle route-based gating.
- Core: Hardened audio preloading with explicit timeouts to prevent background task hangs.
- Testing: Resolved flakiness in audio cache tests by eliminating external network dependencies.
- Maintenance: Expanded auto-approval policies for zero-friction monorepo orchestration.

---

What's new in v1.3.19+229

- TV Enhancement: Finalized formatting and layout stability for the real-time developer monitor.
- Maintenance: Orchestration of the automated /deploy pipeline across the monorepo.

---

What's new in v1.3.18+228
- TV Enhancement: New real-time developer monitor to visualize inactivity thresholds on Google TV.
- TV UI: Hardened inactivity detection to prevent state loss during background source transitions.
- Maintenance: Introduced automated /deploy and /hammer workflows for zero-friction releases.
- Maintenance: Finalized formalized inactivity regression report following deep-dive TV audit.

---

What's new in v1.3.17+227
- TV Enhancement: Refined inactivity detection logic to ignore phantom input signals on Google TV.
- TV UI: Hardened inactivity timer against accidental disablement by system dialogs.
- Web UI: Optimized vertical spacing in the Fruit theme playback screen for consistent 5pt gaps.
- Workflow: Eliminated friction from the automated release and health check pipelines.

---

What's new in v1.3.15+225
- Maintenance: Completed a full monorepo-wide health check with successful analysis and passing tests across all targets.
- Verification: Synchronized the workspace verification status and enforced strict formatting and linting standards.

---

What's new in v1.3.13+223
- Maintenance: Stabilized monorepo test runs by enforcing sequential execution to prevent resource contention.
- Quality: Successfully completed a comprehensive project-wide health check and registered new verification results.

---

What's new in v1.3.12+222
- TV UI Testing: Hardened dual-pane layout stability and automated choice selection.
- Testing Resilience: Fixed layout overlaps in AMOLED "True Black" visual verification.
- Data Quality: Improved track metadata accuracy for Grateful Dead song hints.

---

What's new in v1.3.11+221
- TV UI: Refined visual highlights—now ONLY the active cursor has the rainbow RGB border for maximum clarity.
- TV UI: Added focus memory to the track list; it now remembers your last selected item when returning to the library.
- Web UI: Added a "Crossfade Play/Pause" setting for smooth audio transitions on the Fruit theme.
- Data: Conducted a major cleanup of the Grateful Dead song structural hints, removing JGB and non-GD side projects for accuracy.

---

What's new in v1.3.8+218
- Agent Architecture: Implemented "Smart Verification Receipts" for the shipit workflow, eliminating redundant analyzer and test runs.
- Workflow: Integrated verification receipts into the /checkup workflow to ensure cross-workflow health check persistence.

---

What's new in v1.3.7+217
- TV Enhancement: New InactivityService for more reliable screensaver triggering.
- TV Enhancement: Improved Android Manifest for better Google TV compatibility.
- Testing: Added regression tests for TV inactivity and screensaver flow.

---

- Core (Visualizer): New EKG, Circular EKG, VU, and Oscilloscope modes for screensaver.
- TV Enhancement: High-fidelity stereo capture and multi-algorithm beat detection.
- Web Audio: Fixed player hang issue during rapid state transitions.
- Testing: Hardened gdar_mobile widget tests and fixed monorepo asset resolution.

---

What's new in v1.3.5+215
- Core: Introduced SongStructureHintService and related models for track-specific hint parsing.
- Testing: Fixed asset path resolution in shakedown_core unit tests for monorepo compatibility.
- Testing: Hardened gdar_mobile widget test fakes to prevent NoSuchMethodError during provider updates.

---

What's new in v1.3.4+214
- Visualizer: Added beatSource (PCM/FFT) support for higher fidelity capture telemetry.
- Android: Enhanced VisualizerPlugin.kt to support raw PCM capture on modern TV devices.
- Documentation: Conducted a detailed 2026-03-21 screensaver audio and graph mode audit.
- Maintenance: Resolved curly brace style lints and hardened mobile widget test stability.
- Core: Improved visualizer telemetry parsing with robust numeric clamping and validation.

---

What's new in v1.3.3+213
- Testing: Resolved text stream corruption and hardened unit test isolation.
- Quality: Fixed assertion failures related to new default audio reactivity states, ensuring a 100% clean test pass.

---

What's new in v1.3.2+212
- TV Enhancement: Finalized infrastructure for true stereo L/R VU meters via AudioPlaybackCapture (API 29+).
- TV Architecture: Conducted a comprehensive screensaver audio audit (2026-03-21) to align native detector levels with visualizer telemetry.
- Documentation: Updated audio graph modes, reactivity status, and tuning guides for advanced TV configuration.
- Maintenance: Synchronized codebase formatting and refined performance-tier naming in StealGraph.

---

What's new in v1.3.1+211
- TV Enhancement: Implemented StereoCapture for the Android TV engine, providing high-fidelity PCM capture for real-time visualizers.
- TV Architecture: Added specialized TV banner assets and startup metadata for enhanced Leanback visibility.
- TV Debugging: Expanded TV_DEBUGGING.md with new deep-link automation sequences for rapid UI verification.
- Screensaver: Optimized EKG spread and beat detection sensitivity for a more responsive reactive experience.

---

What's new in v1.3.0+210
- Maintenance: Synchronized codebase with latest monorepo standards and regenerated test mocks.
- Hygiene: Resolved unused imports and synchronized cross-package message routing contracts.
- Agent Architecture: Hardened release and auto-approve protocols for zero-friction orchestration.

---

What's new in v1.2.9+209
- Hygiene: Enforced strict codebase formatting across the monorepo to maintain high standards and readability.
- Maintenance: Verified and synchronized test stubs and contract tests following the latest provider API updates.

---

What's new in v1.2.8+208
- TV Bootstrap: Standardized SharedPreferences injection in GdarTvApp for consistent startup.
- Testing: Hardened TV startup regression tests by addressing timing race conditions.

---

What's new in v1.2.7+207
- Web Audio: Hardened source switching logic in AudioProvider to ignore transient state mismatches.
- Web Audio: Optimized PlaybackScreen list synchronization with safety guards to prevent crashes.
- Architecture: Improved navigation between Library and Playback screens in Fruit theme.
- UI: Refined hit-testing and layout for the FruitNowPlayingCard mini player.
- Testing: Added unit tests for AudioProvider pre-queueing and source synchronization.

---

What's new in v1.2.6+206
- Resolved critical "player hung" issues in the Gapless Web Audio engine.
- Hardened HTML5 fallback engine with improved track disconnection logic.
- Implemented error notification throttling to prevent UI starvation.
- Optimized high-frequency state emission for improved Web performance.
- Fixed unawaited_futures in track list screen for improved code hygiene.

---

What's new in v1.2.5+205
- Enhanced TV screensaver text spacing and squish-to-fit behavior for long track titles.
- Hardened compact playback layouts to avoid header overflow in constrained panel states.
- Standardized TV startup through SplashScreen while preserving the no-onboarding TV boot path.
- Refreshed monorepo regression coverage with smaller, more durable startup and shared-widget tests.

---

What's new in v1.2.4+204
- Branded launcher icons for Android, iOS, and Web platforms.
- Improved App Icon generation workflow with unified melos script.

---

What's new in v1.2.3+203
- Unified Diagnostics HUD with real-time streaming for transparent audio engine telemetry.
- Refactored Playback Messages for improved UI responsiveness and performance.
- Fixed visibility of playback status text when disabled in Settings.
- Resolved duplicate dispose logic and consolidated engine diagnostics.

---

What's new in v1.2.1+201
- Resolved launch failure where the app would hang at the onboarding screen.
- Restored consistent 'Rock Salt' branding on TV across all screens and widgets.
- Fixed scrollbar year chip positioning on Web for accurate navigation.
- Defaulted onboarding to "completed" for Web apps to skip unnecessary steps.
- Relocated core analysis scripts to synchronize with monorepo structure.

---

What's new in v1.2.0+200
- Architecture: Migrated to a Dart workspace monorepo with independent app targets (Mobile, TV, Web) and shared core packages.
- Versioning reset to +200 to mark the new monorepo era.

---

What's new in v1.1.69+169
- Updated device_info_plus and app_links.
- Migrated to hive_ce for improve Dart 3.7+ support.
- Updated flame to v1.36.0 for Game Jam compatibility.
- Updated sliding_up_panel for better stability.
- Fixed Fruit theme activation and glowMode reset.
- Resolved Android compilation errors.

---

What's new in v1.1.68+168
- Resolved MissingStubError in playback_alignment_test.dart by stubbing Fruit theme settings and AudioProvider streams.
- Fixed track title alignment stability test for Material 3.

---

What's new in v1.1.67+167
- UI/UX (HUD): Implemented interactive popup menus for developer HUD chips on Web.
- UI/UX (HUD): Added a STB chip to the HUD for session preset control.
- UI/UX (HUD): Optimized long message display with animated Marquee text.
- UI/UX (Settings): Added automated Relaunch Required notifications for critical settings.
- Deep Links: Added force_tv=true parameter for cross-platform TV UI testing.
- Fixed layout overflow in PlaybackPanel and refined PlaybackProgressBar visuals.
- Resolved regression test failures and NoSuchMethodErrors in TV suite.

---

What's new in v1.1.66+166
- Implemented Smart Coloring for the debug layout, applying distinct functional colors to major sections to improve developer scannability.
- Added granular debug outlines to internal components for high-precision layout inspection.
- Refined the heartbeat dot in the developer HUD, relocated it, and enforced strict hidden state to reduce visual clutter.
- Verified project health and enforced strict formatting/analysis standards via scripts/verify.dart.

---

What's new in v1.1.65+165
- Added custom JavaScript error logger to capture and dump unhandled WebAssembly errors for easier debugging on mobile.
- Removed --wasm requirement from the production deployment pipeline to bypass Skwasm crashes on certain devices.

---

What's new in v1.1.64+164
- Screensaver: Implemented Advanced Audio Reactivity with 8-band frequency isolation.
- UI/UX (TV): Resolved playback message overflow and refined focus highlight aesthetics.
- UI/UX (TV): Improved Show List card layout with larger stars and unified badges.
- Screensaver: Added hidden "Woodstock Every Hour" easter egg functionality.
- Infrastructure: Integrated Puppeteer stress tests for Wasm stability verification.

---

What's new in v1.1.63+163
- Testing: Enhanced tv_settings_screen_test.dart with robust MockAudioProvider and stabilized AboutSection tests.
- UI/UX (TV): Hidden Haptic Feedback and Swipe to Block settings from the TV interface, adhering to platform interaction rules (tv_rules.md).

---

What's new in v1.1.62+162
- Fixed stale UI state in show transitions by ensuring metadata syncs correctly.

---

What's new in v1.1.61+161
- Overhauled ShnidBadge for consistent HTML-style link behavior.
- Fixed missing sourceUrl in RatingDialog from show list view.
- Refined hit-testing for badge links in Fruit-style cards.
- Stabilized track list header layout for better accessibility.
- Reverted experimental Fruit style global mini-player.

---

What's new in v1.1.60+160
- Added cross-platform verify tool and TV hint cards.
- Refined Now Playing card and Track List header.
- Decoupled screensaver track motion for better translation.
- Optimized graph visuals and audio reactivity defaults.
- Hardened screensaver initialization and disposal cycles.
- Fixed native Android beat detection accuracy.
- Reduced Web UI rebuild costs and scroll churn.

---

What's new in v1.1.59+159
- Synchronized browser theme color with PWA background.
- Restored bottom navigation to the Web Track List screen.
- Fixed non-functional navigation in the Fruit Settings header.
- Optimized Web layout by removing redundant players and blurs.
- Updated setup guide with the optimal web-server run command.

---

What's new in v1.1.58+158
- Refined "Text Pixel Snapping" to fix individual letter crawling.

---

What's new in v1.1.57+157
- Added "Text Pixel Snapping" to eliminate sub-pixel text jitter.
- Resolved critical test regressions in playback and screensaver screens.
- Fixed timer leaks in screensaver initialization for stable performance.
- Enforced strict "Default Off" policy for Fruit theme premium features.
- Regenerated all platform mocks to synchronize with latest Provider APIs.
- Updated agent efficiency rules with explicit testing thresholds.

---

What's new in v1.1.56
- Fixed infinite-width crash in segmented controls.
- Upgraded Settings screen header with premium "Liquid Glass" translucency.
- Integrated FruitTabBar bottom navigation directly into the Settings screen.
- Addressed PowerShell pipe compatibility in agent TODOs.

---

What's new in v1.1.55
- Added "Consider Donating to the Internet Archive" link with heartbeat icon.
- Implemented TV "Switch Pane" shortcut and Back-to-master navigation.
- Gated "Fruit" Liquid Glass theme to Web/PWA platforms only.
- Eliminated TV "bounce scroll" glitches via synchronized list keying.
- Added high-pressure CPU stress testing to the automated audio audit.
- Initialized "Size Guard" for ongoing app size and asset optimization.
- Dimmed inactive panes in TV dual-layout for clearer focus.

---

What's new in v1.1.54
- Implemented TV "Switch Pane" shortcut and Back-to-master navigation.
- Gated "Fruit" Liquid Glass theme to Web/PWA platforms only.
- Eliminated TV "bounce scroll" glitches via synchronized list keying.
- Added high-pressure CPU stress testing to the automated audio audit.
- Initialized "Size Guard" for ongoing app size and asset optimization.
- Dimmed inactive panes in TV dual-layout for clearer focus.

---

What's new in v1.1.53
- Implemented "Surgical Stabilization" for Premium Highlights to prevent focus loops.
- Added "Safe-Zone Scrolling" to the track list to minimize UI jumping.
- Resolved "leftover highlights" by explicitly clearing focus on every move.
- Fixed layout shifting by ensuring the RGB border widget stays mounted.
- Optimized RGB border performance with a zero-cost short-circuit.

---

What's new in v1.1.52
- Implemented auto-scrolling for the left panel Show List.
- Updated "Premium Highlight" and playing track borders to the RGB rainbow spectrum.
- Refined playing track border to be a crisp line without glow.
- Changed playback header rating stars to filled yellow for better visibility.
- Fixed several focus and navigation issues on Google TV.
