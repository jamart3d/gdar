# P2 Final Phase: HUD Stabilization & Advanced Continuity

## Phase Goal
Finalize background stability, implement "Soft Stitching" for gapless show transitions, and stabilize the "Fruit" HUD observability.

---

## Arlo Execution Guidelines
- **Surgical Focus**: All logic remains in `apps/gdar_fruit/lib/audio/web_audio_engine.dart`.
- **UI Focus**: Implement HUD layout logic in `packages/shakedown_core` following "Fruit" (Liquid Glass) rules.
- **Analysis**: Ensure the new `GdarHudSnapshot` stream is tested for memory leaks.

---

## 1. Architectural Cleanup: Unified HUD Snapshot
**Task**: Consolidate diagnostics to reduce UI jitter and nested StreamBuilders.
- [ ] **Unified Stream**: Create a `GdarHudSnapshot` class (immutable) that combines `CTX`, `MDFT`, `RVC`, and `SR` (Sample Rate).
- [ ] **HUD Stabilization**: Implement fixed-width constraints for the `MEM`, `DEC`, and `SCH` chips so the layout doesn't "jump" during value updates.

---

## 2. Advanced Continuity & System Sync
**Task**: Handle show transitions as continuous segments and sync with the OS.
- [ ] **MediaSession Sync**: Ensure `playbackState` and metadata update immediately during "Soft Stitch" transitions.
- [ ] **Soft Stitching**: Treat the boundary between shows as a virtual continuity segment to prevent 0ms gap overlaps.
- [ ] **Fallback Glue**: Inject a short silent "Glue Track" if stability drops below threshold during a transition.

---

## 3. Final Verification
- [ ] **Logic Proof**: Verify the `GdarHudSnapshot` emits exactly one event when multiple internal values change.
- [ ] **Stitch Test**: Mock a "Show End" and verify the Boundary Sentinel correctly seeds the next show.

---

## Final Compliance Checklist (Jules Sign-off)
Before marking the Web UI Improvements as COMPLETE, verify the following:

- [ ] **Fruit Rule**: No Material 3 widgets, ripples, or FABs exist on any Web/PWA screen.
- [ ] **Clean Architecture**: UI is decoupled from logic; state management is strictly via Provider.
- [ ] **Deprecation Check**: All code uses latest stable Flutter (e.g., `.withValues()` instead of `withOpacity()`).
- [ ] **Isolation**: Audio engine logic is 100% platform-agnostic at the interface level.
- [ ] **Monotonicity**: All drift and timing calculations use the monotonic performance clock.