---
description: Perform a health check of the codebase (analyze, test, format).
---

// mcp
1. Use `mcp_dart-mcp-server_analyze_files` to check for static analysis issues across the project.
// mcp
2. Use `mcp_dart-mcp-server_run_tests` to ensure all tests pass.
// mcp
3. Use `mcp_dart-mcp-server_dart_format` to ensure consistent code styling.
