---
description: Perform a health check of the codebase (analyze, test, format).
---

1. Run `dart analyze` to check for static analysis issues.
2. Run `flutter test` to ensure all tests pass.
3. Run `dart format .` to ensure consistent code styling.
