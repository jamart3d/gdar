---
description: Reconnect to Android device via ADB using phone_soft
---

1. Kill existing ADB server
   ```bash
   adb kill-server
   ```

2. Run phone_soft to reconnect
   // turbo
   ```bash
   bash ~/bin/phone_soft
   ```
