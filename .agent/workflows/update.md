---
description: Pull latest changes from remote repository
---

1. Run `git status` to check for local modifications.
2. If the directory is not clean (modified files), STOP and inform the user that they should stash or commit changes first.
3. If clean, run `git pull` to fetch and merge the latest changes.
