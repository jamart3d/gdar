---
# Task: Monorepo Phase 1 - Infrastructure & Models
1. Create `melos.yaml` for `packages/*` and `apps/*`.
2. Create `/packages/shakedown_core`.
3. Move simple data models (Show, Track, Rating) from `lib/models/` to `packages/shakedown_core/lib/models/`.
4. Update all imports in the root project to point to `package:shakedown_core/models/...`.
5. Ensure the app still builds.
---
